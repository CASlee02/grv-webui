apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: open-webui
  name: open-webui-pvc
  namespace: grv-webui
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 8Gi